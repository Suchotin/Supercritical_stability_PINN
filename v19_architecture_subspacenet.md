# v19/v19b — бессеточный нейрооператор устойчивости: архитектура, проблема выбора моды и почему выбран SubspaceNet + подпространственная итерация + Рэлей–Ритц

Документ описывает конструкцию ноутбуков `pinn_v19_subspace.ipynb` (первая версия, отрицательный результат для SubspaceNet) и `pinn_v19b_subspace_power.ipynb` (рабочая версия), её место в проекте, математику каждого блока и — главное — природу проблемы, ради которой появился SubspaceNet, и обоснование принятого решения со ссылками на литературу. Все числа — из прогонов 2026-08-21 (K_out = 20, N_SPC = 2.0, эталон TempaFV(r_eos)).

---

## 0. Зачем это нужно: краткая предыстория в одном абзаце

Вся линия v4–v17 измерила одно и то же: нейросеть, которая **представляет решение** (поля (G, h, Π)(z, t) или шаг u_n → u_{n+1}), не воспроизводит рождение DWO иначе как вырождаясь в численный интегратор (v12 — жёсткая связка состояний, колебания есть, сеть = решатель шага; глобальные/мягкие формулировки — гладкая ветвь). Причина структурная (тест допустимости v7b, игрушка v18): гладкая квазистационарная ветвь — точное решение уравнений при любых параметрах, внутри любого окна по времени она дешевле по лоссу, чем достижимое представление растущей осцилляции, а «выбор ветви» делает только динамика (степенной метод по времени), которой у минимизации нет. Отсюда смена постановки: сеть должна представлять только **гладкие объекты** — квазистационарную ветвь и **линейную устойчивость вокруг неё** (инкремент α(t), частоту ω(t), форму моды ψ(z), порог α = 0), а рост и осцилляция берутся из анзаца аналитически. Линейный анализ устойчивости — классика гидродинамики (Schmid & Henningson, 2001); новизна — в том, что и ветвь, и подпространство мод выдаёт сеть, непрерывно по z (бессеточно, на любом облаке частиц) и по t, а в следующем шаге — по параметрам режима.

---

## 1. Общая схема

```
(z, t)  ──► BranchNet (v13, upwind-LSFVP, заморожена) ──► u_s(z,t) = (G_s, h_s)          ◄── гладкая ветвь, 0.31 % от TempaFV
                                                          │
                    состояние на облаке частиц  U(t) = ( h_s(z_i) [N],  G_in(t),  ∫G_s dz )      ◄── любое облако: регулярное/джиттер/другое N
                                                          │
                    LSFVP-оператор v13 (та же невязка, что в её лоссе) как функция (U, U_t):  R_c(U, U_t) ∈ ℝ^{N+2}
                                                          │
                    линеаризация на ветви:  M = ∂R_c/∂U_t ,  F = ∂R_c/∂U  (автоград)  →  пучок  (λM + F)·v = 0
                    [в прогоне v19b ∂/∂t в операторе взят разностью (U₁−U₀)/Δt ⇒ отображение J = −A⁻¹B, λ = ln μ/Δt — см. §3]
                         │                                              │
          ┌──────────────┴───────────────┐               ┌──────────────┴──────────────────────┐
   ТОЧНЫЙ ОРАКУЛ:  полный спектр (130×130) │               │  SubspaceNet Q(z,t) ∈ ℝ^{(N+2)×K}, QR   │  ◄── обучается подпространственной
   → α, ω, ψ   (валидация)                │               │  Ритц:  проекция пучка на span Q, K×K   │      итерацией (цель orth(Jᵐ Q))
          └──────────────┬───────────────┘               └──────────────┬──────────────────────┘
                         └──────────── сравнение: t*(α=0), период 2π/ω, sinΘ(ψ, span Q) ─────────┘
```

Три компонента: **ветвь** (сеть v13), **линеаризованный LSFVP-оператор** (тот же, что в лоссе v13; не сеть), **подпространство** (сеть) + **извлечение моды** (линейная алгебра K×K). Оракул — полный спектр линеаризации на том же облаке; он же — v15-детектор, перенесённый с сетки TempaFV на облако и с исправленной трактовкой состояния (см. §6).

---

## 2. Компонент 1 — гладкая ветвь: сеть v13 (upwind-LSFVP)

**Метод.** LSFVP по Zhang, Liu, Chen (EABE 187, 2026, 106747) — PINN, у которого невязки считаются не поточечно (автоград по z), а **интегрально по контрольным объёмам вокруг частиц**, с реконструкцией значений на гранях МНК-полиномами Type-B (LSMPS: r_e = 3.1h, r_s = 0.2h, p = 2). Наша единственная правка к статье — **конвективные потоки берутся с наветренной стороны** (донорная частица, P_CONV = 0), а не центральной реконструкцией: измерено (v13, хвостовая ячейка), что только так лосс допускает осциллирующую истину (невязка истины TempaFV 10⁻¹¹ против 3.7·10⁻³ при p ≥ 1 у входного скачка h); что пространственный LSFVP-оператор с донорными потоками содержит неустойчивость, показывает его спектр (§3): ведущая пара переходит в правую полуплоскость с периодом 5.7–5.9 s.

**Сеть.** `SmoothPINN`: (z, t) → масштабирование в [−1, 1] → 6 × tanh(128) → (Ĝ, ĥ, Π̂); поля — через жёсткий анзац:
```
h(z,t) = −N_SPC + N_SPC · z · ĥ(z,t)        (h(0) = −N_SPC тождественно)
G(z,t) = G0* · Ĝ(z,t)                         (расход свободен; IC G = холодный стационар при t = 0 — в лоссе)
```
**Лосс** — три интегральных баланса на CV вокруг 128 частиц (масса, энергия — с донорными потоками `G`, `G·h` на гранях; импульс — интегральный по каналу, структура TempaFV: `d/dt ∫G dz = Δπ* − dpc`, dpc = гравитация + Blasius-трение + местные потери), ∂/∂t — автоград сети; веса mass/energy/mom = 2/0.5/20; Adam 40 000 ит + L-BFGS; чекпоинт `v13_upwind_lsfvp_up.pt`.

**Что даёт и почему используется как есть.** Огибающая расхода 0.31 % от TempaFV(r_eos) на t < 1750 s — лучший результат гладкой ветви среди всех глобальных формулировок проекта (v6/v8 0.44–0.50 %, v13c 0.49 %, v16 0.5–1.5 %). Глобальная минимизация по времени сама садится на гладкую ветвь — здесь это **желаемое** поведение: ветвь нужна и за порогом (неустойчивый стационар — именно то, вокруг чего линеаризуют). Ветвь замораживается; дальше она только вычисляется в частицах.

---

## 3. Компонент 2 — линеаризованный LSFVP-оператор (тот же, что в лоссе v13)

Это не сеть, а физика на облаке — **та же невязка, которую минимизировала v13**, только теперь не минимизируется, а линеаризуется вокруг готовой ветви. Сеть её не должна ни заменять, ни размывать.

**Состояние** (на облаке из N частиц): `U = (h₁…h_N, G_in, I)`, где `I = ∫G dz` — интегральный импульс. Включение `I` в состояние принципиально: импульс интегрируется своим уравнением, а `G(z) = G_in − Σ ρ_t V` содержит ρ_t, поэтому система для (h, G_in) второго порядка по времени; с `I` как независимой переменной система первого порядка (в v15 `I₀ ≈ G_in₀` — отброшено ρ_t — и это сдвинуло порог, см. §6).

**Оператор** — невязка LSFVP v13 как функция состояния и его производной по времени (донорные МНК-потоки на гранях, интегральный импульс TempaFV-структуры, Blasius, r_eos):
```
ρ_t = ρ_h(h)·h_t ;      G(z_i) = G_in − Σ_{j≤i} ρ_t,j V_j                       (неразрывность — тождество)
R_E,i = (ρ h)_t,i + [ (G h)_{i+½} − (G h)_{i−½} ] / V_i − N_Q'(t)                (донорные (G h) на гранях; приток G_in·h_in)
R_M   = I_t − ( Δπ* − dpc(G, h, G_in) )                                         (интегральный импульс)
R_I   = I − Σ_i G(z_i) V_i                                                       (определение I)
R_c(U, U_t) = [ R_E / energy_scale ; R_M / Δπ* ; R_I ] ∈ ℝ^{N+2}   — квадратная система
```
**Линеаризация (целевая форма).** Возмущение `δU = v·e^{λt}` вокруг ветви u_s(t) даёт обобщённую собственную задачу — пучок:
```
( λ·M + F )·v = 0 ,      M = ∂R_c/∂U_t |_{u_s} ,   F = ∂R_c/∂U |_{u_s}      (автоград; 130×130 при N = 128)
α = Re λ (инкремент, 1/s),  ω = Im λ,  период 2π/ω,  порог α(t*) = 0
```
Без дискретизации по времени: λ получается напрямую. Это форма для v19c.

**Как было в прогоне v19b.** Для скорости сборки ∂/∂t в операторе взят разностью между двумя состояниями ветви: `U₀ = u_s(t)`, `U₁ = u_s(t+Δt)`, `R(U₁,U₀) = R_c(U₁, (U₁−U₀)/Δt)` (неявная аппроксимация), откуда `A = ∂R/∂U₁`, `B = ∂R/∂U₀`, отображение `J = −A⁻¹B` и `λ = ln μ/Δt`. Это **аппроксимация того же пучка** (при малом Δt `J ≈ (M + ΔtF)⁻¹M` — преобразование Кэли пучка, у которого те же собственные векторы), и у неё есть артефакт — численное демпфирование: `α_Δt ≈ α − ω²Δt/2`. Измерено: α(1790 s) = 0.118 / 0.089 / 0.031 1/s при Δt = 0.05 / 0.1 / 0.2 s (разности совпадают с ω²Δt/2 при ω ≈ 1.1 рад/s), период устойчив (5.69 / 5.65 / 5.61 s); экстраполяция Ричардсона даёт непрерывные α(1790) = 0.148, α(1820) = 0.198 и порог ≈ 1700 s (детектор при Δt = 0.1 s показывает 1727 s). Все сравнения «Ритц vs оракул» в v19b сделаны при одном Δt, поэтому на валидацию метода артефакт не влияет; на абсолютный порог — влияет, и пучок его снимает.

**Оракул.** Полный спектр (eig пучка или eig(J)) упорядочивается по Re λ тривиально. Спектр у порога чист: при 1810 s ведущая пара `0.119 ± 1.095i` (период 5.74 s), следующие `−1.20 ± 4.36i`, `−2.94 ± 6.29i` — большой зазор, что важно для §4.4.

---

## 4. Компонент 3 — SubspaceNet: задача, проблема, почему не «целиком сетью», выбранный вариант

### 4.1. Что именно нужно

Для каждого t (и в перспективе для каждого режима p) нужны ведущая собственная пара (λ_lead, ψ_lead) несамосопряжённого оператора `J(t; p)` и порог α = 0 — **непрерывно по t, p и по z** (бессеточно), дёшево на запрос. Полный `eig` даёт точный ответ в точке, но для каждой точки нужно собрать оператор и решить 130×130 задачу; непрерывности по параметрам у него нет. Сеть могла бы дать амортизацию: один forward → подпространство → K×K задача → ответ.

### 4.2. Проблема: выбор моды (mode-selection degeneracy) и почему она возникает

Естественная «PINN-формулировка» собственной задачи — обучаемая пара (λ, ψ_θ) и лосс
```
L_eig = ‖ J ψ_θ − λ ψ_θ ‖²   при   ‖ψ_θ‖ = 1   (нормировка — чтобы исключить ψ = 0)
```
Её глобальный минимум (ноль) достигается на **каждой** из N+2 собственных пар. Для самосопряжённых операторов это лечится упорядочиванием: спектр вещественный, есть вариационный принцип Рэлея (минимум/максимум отношения Рэлея — крайняя мода), дефляция ортогональностью к уже найденным модам даёт следующие (Jin, Mattheakis, Protopapas, 2022; Yang et al., 2023 — сети на идее степенного/обратного степенного метода; Pfau et al., 2019 — SpIN; Deng, Shi, Zhu, 2022 — NeuralEF; ускоренная биконвексная версия — arXiv 2511.00792, 2025). У нас оператор **несамосопряжённый и ненормальный** (перенос + запаздывающая обратная связь): спектр комплексный, отношения Рэлея комплексны — «минимизировать» их нельзя (так прямо и сформулировано в Deep Eigenspace Network, arXiv 2512.20058: для несамосопряжённых задач вариационные PINN-лоссы неэффективны); собственные векторы чувствительны к возмущениям и переключаются у кластеров/пересечений (там же; общая теория — Trefethen & Embree, 2005; Stewart & Sun, 1990). Практика loss-based решателей это подтверждает: метод сходится к паре, **ближайшей к начальной оценке** (arXiv 2511.00792), — т.е. выбор моды делает инициализация, а не физика.

**Подъём на уровень подпространства не снимает вырожденность.** В v19 я поставил лосс инвариантности
```
S = −(QᵀAQ)⁻¹(QᵀBQ),     L_inv = ‖ B Q + A Q S ‖²_F / ‖ B Q ‖²_F      (эквивалент ‖JQ − QS‖ без обращения A)
```
Он равен нулю на **любом** инвариантном подпространстве — любом наборе K собственных векторов. Измерено: L_inv сошёлся к 6·10⁻⁵, но угол к ведущему собственному вектору sinΘ = 0.21, порог Ритцем не найден. Это та же вырожденность, что у одиночной моды, — оптимизатор нашёл ближайшее к инициализации инвариантное подпространство, и оно ведущую моду не содержало.

**Почему в игрушке v18 и в численном решателе этой проблемы нет.** В игрушке `x' = −a x − b x(t−τ)` мы решали задачу Коши: история x ≡ ε фиксирует амплитуды *всех* мод (проекция начальных данных на собственные функции), невязка заставляет λ_k быть корнями, растущая мода присутствует обязательно — вопроса «какую моду искать» нет. Численный интегратор тоже не ищет моды: динамика сама делает степенной метод по времени, и ведущая мода побеждает из шума. А `eig` выдаёт все моды разом — сортировка бесплатна. Проблема выбора появляется **только** когда моду ищут градиентным спуском по статическому функционалу без упорядочивающего принципа.

### 4.3. Почему нельзя решить «целиком нейросетью»

1. **Нет статического функционала, единственный минимум которого — ведущая мода несамосопряжённого оператора, без супервизии.** Упорядочивание по Re λ — дискретное, комбинаторное свойство спектра; гладкий лосс на (ψ, λ) или на Q его не кодирует (§4.2). Суррогаты вроде «максимизировать Re λ_Ritz» небезопасны для ненормальных операторов: Ритц-значения на неинвариантном Q лежат в поле значений (numerical range), которое шире спектра — можно «выиграть» большое Re за счёт транзиентного роста, не попав ни в одну моду (Trefethen & Embree, 2005).
2. **Супервизия точными парами** (как в DEN: обучение на FEM-данных, цель — кластер мод внутри контура в комплексной плоскости) решает выбор, но превращает сеть в интерполятор чужих решений — физической (PINN-) формулировки не остаётся; кроме того, сами отдельные собственные векторы как цель негладки по параметрам (переключения мод) — DEN поэтому учит **подпространство**, а не моды.
3. **Учить динамику и дифференцировать** (нейрооператорный фреймворк устойчивости arXiv 2604.19465: эмулятор шага → якобиан автоградом → обычная `eig`) — спектр всё равно считается линейной алгеброй, а эмулятор шага в нашем случае компаундит (v17: чистый forward расходится за 42 s).
4. **Решение задачи Коши для возмущения сетью** (чтобы динамика сама выбрала моду, как в игрушке) — возвращает нас к тому, что доказано не работает: глобальная минимизация по времени нейтральна к неустойчивости (v10b: затравка ×1.0 против ×60 у интегратора).

Вывод: часть «какая мода ведущая» должна делаться либо действием оператора (итерацией), либо линейной алгеброй; сети остаётся то, что она делает хорошо, — гладкое представление подпространства по (z, t, p).

### 4.4. Выбранный вариант: нейросетевая подпространственная итерация + Рэлей–Ритц

**Математическое основание.** Одновременная (подпространственная, ортогональная) итерация `Q ← orth(J Q)` для несамосопряжённых матриц (Stewart, 1976; Saad, 2011, гл. 5; Golub & Van Loan, 2013, §7.3) сходится к **доминирующему инвариантному подпространству** — натянутому на K собственных векторов с наибольшими |μ| — со скоростью `|μ_{K+1}/μ_K|` на итерацию; оно единственно при ненулевом зазоре. Для Кэли-отображения пучка `|μ| = e^{α Δt}` монотонно по α, поэтому «доминирующее по |μ|» = «наибольшие инкременты» — ровно то, что нужно, и без упорядочивания комплексных чисел. На инвариантном подпространстве проекция Галёркина точна: если `JQ = QS`, то `QᵀJQ = S` и собственные числа S — собственные числа J (Saad, 2011, гл. 4); при малом угле Θ между span Q и инвариантным подпространством ошибка собственных чисел линейна по sinΘ (оценки типа DEN, Thm 10; Stewart & Sun, 1990).

**Архитектура SubspaceNet** (K = 6):
```
вход:        z ∈ [0,1] (частицы облака), t (масштаб.), [в следующем шаге — p = (N_SPC, K_out)]
h-часть:     RFF(z; m = 16, σ = 2) ⊕ z ⊕ t  →  MLP 3 × tanh(64)  →  K значений  q_k(z, t)      (непрерывные функции z)
скаляры:     MLP(t) 2 × tanh(32)  →  2K значений                          (компоненты по G_in и по I для каждой q_k)
сборка:      Q_raw(t) = [ q_k(z_i) ; q_k^{G_in} ; q_k^{I} ]  ∈ ℝ^{(N+2)×K}  на текущем облаке
нормировка:  Q = qr(Q_raw)  —  жёсткая ортонормировка на облаке внутри forward (нет штрафов за норму/ортогональность)
```
Параметров ~20 k. Облако — любой аргумент: Q на джиттер-облаке или облаке другого N — это **та же непрерывная функция**, сэмплированная в других точках; МНК-операторы граней и якобианы A, B при этом пересобираются для нового облака (это и есть бессеточность: сеть не привязана к дискретизации, дискретизация пересчитывается вокруг неё).

**Лосс (v19b):**
```
цель степенного метода:  T = orth( Jᵐ Q )   (m = 16 применений J = (M+ΔtF)⁻¹M — Кэли-отображения пучка, у него те же собственные векторы; без градиента)
расстояние подпространств:  L_pow = 1 − ‖ Qᵀ T ‖²_F / K   ∈ [0, 1]     (= ‖QQᵀ − TTᵀ‖²_F / 2K)
инвариантность:             L_inv = ‖ B Q + A Q S ‖²_F / ‖ B Q ‖²_F,   S = −(QᵀAQ)⁻¹(QᵀBQ)
полный:                     L = L_pow + L_inv ,   усреднение по батчу 32 моментов t из банка 400 (t ∈ [1000, 2060] s)
```
Смысл: на каждой итерации Adam сеть подтягивается к тому, куда её текущее Q сдвинула бы подпространственная итерация; неподвижная точка процесса — единственное доминирующее подпространство (в отличие от неподвижных точек одного L_inv). L_inv оставлен как регуляризатор «чистоты» инвариантности. Банк `(A, B, J)` для 400 моментов собирается один раз (11 s); в форме пучка — `(M, F)` и `J = (M+ΔtF)⁻¹M` как оператор итерации; обучение 4000 ит — 560 s на H100 (узкое место — 32 × 16 QR в питоновском цикле, не сеть). Матрично-свободная версия возможна (J·q через jvp по невязке и итерационное решение с A), на размерности 130 нужды нет.

**Извлечение моды (Рэлей–Ритц, линейная алгебра):**
```
форма пучка (v19c):   (QᵀMQ)·λ·c = −(QᵀFQ)·c   (K×K обобщённая) → λ_k, c_k напрямую
форма v19b:           S = −(QᵀAQ)⁻¹(QᵀBQ)  (K×K) ;   eig(S) → μ_k, c_k ;   λ_k = ln μ_k / Δt ;
ведущая: k* = argmax Re λ_k ;   α = Re λ_k*,  ω = |Im λ_k*|,  ψ = Q c_k* ;   порог: α(t*) = 0 (интерполяция по t)
```
Выбор моды происходит среди K = 6 Ритц-значений упорядочиванием вещественных чисел Re λ — здесь это законно и бесплатно.

**Что проверялось и что получено (v19b).**
| облако | t* Ритц vs eig | период Ритц vs eig | sinΘ(ψ_lead, span Q) у порога | L_inv у порога |
|---|---|---|---|---|
| регулярное 128 (обучение) | 1731.7 vs 1727.4 s (Δ +4.2 s) | 5.87 vs 5.84 s (−0.6 %) | 0.003 | 2.8·10⁻⁵ |
| джиттер 0.45h, 128 (без переобучения) | 1732.1 vs 1727.3 s (Δ +4.9 s) | 5.87 vs 5.83 s | 0.006 | 1.2·10⁻² |
| 200 частиц, джиттер 0.3h (без переобучения) | 1730.0 vs 1721.7 s (Δ +8.3 s) | 5.87 vs 5.81 s (−1.0 %) | 0.003 | 4.8·10⁻³ |

Ворота (объявлены до прогона): |Δt*| ≤ 10 s, |Δω/ω| ≤ 5 %, sinΘ ≤ 0.05 — все PASS; форма ψ_h(z) Ритца совпадает с точным вектором (Re и Im); α(t) и период на трёх облаках — одна кривая. Контраст с v19 (L_inv без L_pow): sinΘ 0.21, порог не найден — измеренное подтверждение §4.2.

**Режимы отказа и защита.** (a) Схлопывание зазора |μ_K|≈|μ_{K+1}| у пересечений мод — замедляет итерацию; K = 6 покрывает кластер ведущих мод с запасом (спектр у порога: 0.12, −1.2, −2.9 по Re — зазор большой). (b) Рост L_inv на чужом облаке (до 10⁻²) — след того, что QR-нормировка и МНК-операторы зависят от облака; на α, ω это не повлияло (Δ ≤ 8 s), но при сильно другом N стоит дообучить по банку нового облака. (c) Ведущая мода вещественная (монотонная) вместо пары — argmax Re λ её и вернёт; физически в нашем диапазоне ведущая всегда пара. (d) Разностная аппроксимация ∂/∂t в операторе (v19b) демпфирует α одинаково для Ритца и оракула; для абсолютных значений — форма пучка (§3) или Ричардсон по Δt.

---

## 5. Роль сети и что она даёт против «просто eig»

Честное сравнение. В одной точке (t, p) при N ~ 130 полный `eig` — точен и занимает миллисекунды; SubspaceNet его по точности не превосходит и не должен — он **валидируется** им. Что даёт сеть: (1) непрерывность по z — мода и подпространство как функции, вычислимые на любом облаке (проверено джиттером и N = 200); (2) непрерывность по t — один forward вместо пересборки якобиана в каждой точке; (3) в следующем шаге — непрерывность по параметрам режима p: ветвь v16 уже показала, что одна сеть держит 5 режимов на 0.5–1.5 %, SubspaceNet(z, t; p) даёт тогда α(t; p), ω(t; p), t*(p) для всей области одним forward — карта границы устойчивости без единого марша. Это амортизация и гладкость по параметрам (именно за это DEN учит подпространство, а не моды), а не «точнее численного».

---

## 6. Физический побочный результат: ревизия порога

Точный спектр на облаке даёт t*(α = 0) = 1727.8 s (ветвь сети) / 1726.4 s (ветвь TempaFV на облаке) / 1733 s (60 частиц) при разностном ∂/∂t с Δt = 0.1 s, непрерывный предел (пучок) ≈ 1700 s — против 1804 s у v15 (сетка 60, состояние без I). Разрешение исключено (60 и 128 частиц согласны), ветвь исключена (сеть и TempaFV согласны до 1.4 s). Решающий тест на **явном TempaFV** (`seed_check.py`): затравка I·(1+10⁻⁴) при 1700 s растёт монотонно 7.5·10⁻⁷ → 1.1·10⁻³ (1789 s) → 7.3·10⁻³ kg/s (1801 s) с инкрементом 0.03 → 0.157 1/s; при пороге 1804 s она бы затухала. Следствие: видимые onset (бейзлайн TEMPA-SC 1760 s, TempaFV 1801 s, v12 1819 s) — это линейный порог **плюс время роста из шума** (∫α dt ≈ 7–9 e-фолдов), зависящее от шума решателя; N_TPC на линейном пороге 3.15–3.26 против табличных 3.35 (onset-типа величина). Перед использованием в статье нужно сверить определение порога у Churkin (TEMPA-SC description).

---

## 7. Ограничения и следующий шаг

- Линейная стадия: конструкция даёт рождение неустойчивости (порог, инкремент, период, форма моды), не глубокий предельный цикл с реверсом — для него нужен многомодовый/Флоке-анзац.
- Ветвь одно-режимная (v13); параметрический шаг требует переобучения upwind-LSFVP на батче режимов (часы), SubspaceNet(z,t;p) поверх — минуты; сборку режимов N_SPC ≥ 2.25 и K_out ≠ 20 в TempaFV нужно разобрать отдельно (v14/v16).
- Масштабирование на большие N — матрично-свободная версия (jvp + итерационные решения) вместо банка матриц.
- v19c: перейти от разностной аппроксимации ∂/∂t в операторе к пучку (λM + F) — снять артефакт демпфирования, получить α и порог напрямую; Кэли-отображение пучка остаётся инструментом подпространственной итерации.

---

## 8. Литература

Проектные источники
- Ambrosini W. *Annals of Nuclear Energy* 38 (2011) 615–627 — безразмерные уравнения, BC (imposed Δp, свободный расход).
- Churkin A. TEMPA-SC benchmark description (`Churkin-Description.pdf`), табл. 2 порогов N_TPC.
- Zhang, Liu, Chen. Least-squares finite-volume PINN (LSFVP), *Eng. Anal. Bound. Elem.* 187 (2026) 106747 — Type-B МНК-реконструкция, интегральные невязки (`pinn_v13_*`).
- Chen Z. et al. Fourier time-sequential PINN, *Adv. Eng. Informatics* 72 (2026) 104446 — марш с мягким швом, RFF (`pinn_v7_*`).
- Raissi M., Perdikaris P., Karniadakis G.E. *J. Comput. Phys.* 378 (2019) 686–707 — discrete-time PINN (разд. 4), основа жёсткой связки состояний (v12).
- Wang S., Sankaran S., Perdikaris P. Respecting causality for training PINNs, *CMAME* 421 (2024) 116813 — причинное взвешивание (v4).

Нейросетевые решатели собственных задач и их ограничения
- Jin H., Mattheakis M., Protopapas P. Physics-informed neural networks for quantum eigenvalue problems, IJCNN 2022, arXiv:2203.00451 — norm-loss, ortho-loss/дефляция, самосопряжённый случай.
- Yang Q. et al. Neural networks based on power method and inverse power method for solving linear eigenvalue problems, *Comput. Math. Appl.* (2023), arXiv:2209.11134 — степенной метод как нейросетевой лосс (наибольшее/наименьшее собственное число).
- Fast PINN eigensolvers via biconvex reformulation (PINN-ACS), arXiv:2511.00792 (2025) — самосопряжённые операторы; наблюдение «сходится к паре, ближайшей к начальной оценке»; спурийные бассейны.
- Pfau D., Petersen S., Agarwal A., Barrett D., Stachenfeld K. Spectral Inference Networks, ICLR 2019, arXiv:1806.02215; Deng Z., Shi J., Zhu J. NeuralEF, ICML 2022 — top-k собственных функций самосопряжённых ядер.
- Deep Eigenspace Network for parametric non-self-adjoint eigenvalue problems, arXiv:2512.20058 (2025) — невозможность вариационных PINN-лоссов для комплексного спектра, переключение мод, обучение инвариантного подпространства + Рэлей–Ритц, липшицевость спектральных проекторов по параметрам.
- A neural operator framework for data-driven discovery of stability and receptivity, arXiv:2604.19465 (2026) — эмулятор динамики → якобиан автоградом → спектр линейной алгеброй; ведущие моды через POD-усечение и ансамбли.
- Utilizing curriculum learning for high-frequency eigenfunction discovery through a PINN, *J. Comput. Des. Eng.* (2026), doi:10.1093/jcde/qwag056 — борьба со спектральным смещением и спурийными модами.

Линейная алгебра и теория устойчивости
- Stewart G.W. Simultaneous iteration for computing invariant subspaces of non-Hermitian matrices, *Numer. Math.* 25 (1976) 123–136.
- Saad Y. *Numerical Methods for Large Eigenvalue Problems*, 2nd ed., SIAM 2011 — подпространственная итерация, проекции Галёркина/Рэлея–Ритца.
- Golub G.H., Van Loan C.F. *Matrix Computations*, 4th ed., 2013 — ортогональная итерация, QR.
- Bai Z., Demmel J., Dongarra J., Ruhe A., van der Vorst H. *Templates for the Solution of Algebraic Eigenvalue Problems*, SIAM 2000.
- Stewart G.W., Sun J.-G. *Matrix Perturbation Theory*, 1990 — возмущения инвариантных подпространств, углы Θ.
- Trefethen L.N., Embree M. *Spectra and Pseudospectra*, Princeton 2005 — ненормальность, поле значений, транзиентный рост.
- Schmid P.J., Henningson D.S. *Stability and Transition in Shear Flows*, Springer 2001 — линейный анализ устойчивости, Орр–Зоммерфельд как эталон несамосопряжённой задачи.
- Schmid P.J. Dynamic mode decomposition of numerical and experimental data, *J. Fluid Mech.* 656 (2010) — спектр из данных (родственный подход, контраст с физическим оператором).
