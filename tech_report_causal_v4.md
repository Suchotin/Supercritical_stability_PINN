# Тех-репорт: causal-training PINN (v4) — `good_enough` vs `new`

**Дата:** 2026-06-14
**Файлы:** `pinn_v4_causal_good_enough.ipynb` (победитель) vs `pinn_v4_causal_new.ipynb` (регресс)
**Вопрос:** почему `good_enough` лучше `new`, что означают параметры и `w_min`, и что именно
добавлено из статьи Wang–Sankaran–Perdikaris, *«Respecting causality for training PINNs»*
(CMAME 2024, arXiv:2203.07404)?

---

## 1. Что добавлено из статьи (с формулами)

Обычный forward-PINN минимизирует невязку PDE сразу на **всех** временах. NTK-bias тогда тянет сеть
к тому, что проще подогнать везде одновременно — для нашего сверхкритического канала это гладкая
квазистационарная ветка, а вовсе не density-wave неустойчивость, которая растёт только в позднем
хвосте. Лечение Wang+2024 — **явно упорядочить время**: позднее время «включается» в лосс только
после того, как сошлись все более ранние. Конкретно:

**(1.1) Сортированная сетка времён.** Времена коллокаций — строго возрастающая сетка
`t_0 < t_1 < … < t_Nt` на `(0, t_end]`. В коде: `Sampler.causal_time_grid(Nt)` (linspace-ячейки +
интра-ячейковый jitter, сохраняющий порядок). `Nt` = число временных слоёв (здесь 96).

**(1.2) Поточечная невязка на время.** Для каждого слоя `t_i` усредняем невязку PDE по пространству:

```
L_r(t_i) = (1/Nz) Σ_z [ w_mass·R_mass(z,t_i)² + w_mom·R_mom(z,t_i)² + w_energy·R_energy(z,t_i)² ]
```

(`w_mass,w_mom,w_energy = 2.0, 0.25, 0.5`; `Nz` точек по пространству на слой). Это
`Physics._per_time_residual`.

**(1.3) Начальное условие как сегмент `t_0`.**

```
L_0 = λ_ic · L_ic ,   λ_ic = causal_ic = 1000
```

`L_ic` = рассогласование (G,h) с физическим IC при t=0. `L_0` засевает причинный кумулятив ниже,
поэтому он гейтит *каждый* последующий вес — именно загон `L_ic → 0` сначала «открывает» фронт.

**(1.4) Причинные веса (ядро идеи, Eq. 3.2):**

```
w_i = exp( −ε · Σ_{k<i} L_r(t_k) )        кумулятив засеян L_0
```

со **stop-gradient** на `w_i` (`.detach()` — веса планируют лосс, но сами не оптимизируются).
Чтение: вес времени `t_i` экспоненциально затухает по *суммарной невязке всего, что было до него*.
Если ранние времена плохо подогнаны (большое `Σ_{k<i} L_r`), вес позднего времени ≈0 → оно ничего
не вносит → оптимизатор вынужден сперва починить ранние. По мере падения ранних невязок поздние веса
растут, и «фронт» активных времён марширует вперёд по времени. `ε` (`eps`) — **сила причинности**:
насколько резко режет гейт.

**(1.5) Взвешенный лосс (Eq. 3.3–3.4):**

```
L = L_0  +  (1/Nt) Σ_i  w_i · L_r(t_i)   [ + равномерный BC + каузализированные keeper'ы ]
```

(сверено 1:1 с репозиторием авторов `KS/regular_KS.py`: `loss = mean(W*L_t) + L_0`).
Две адаптации под проект:
- **BC НЕ гейтится** — imposed-Δp это стоячее ограничение в каждый t, добавлен равномерным полным
  весом вне кумулятива (если его гейтить, высоко-ε веса в хвосте выключают выходной BC).
- **Интегральные keeper'ы (mom/energy/mass) каузализированы** — умножены на те же `w_i`, чтобы
  «ехать» на фронте, но держатся ВНЕ кумулятива (фронт задают только PDE+IC).

**(1.6) ε-annealing (Algorithm 1).** Одного ε мало: слишком малое не гейтит, слишком большое режет
ещё не сошедшуюся область насмерть. Поэтому ε метут от малого к большому; для каждого ε делают `S`
шагов; переходят к следующему ε, когда **вся область сошлась**, по критерию `min_i w_i > δ`
(`causal_delta = 0.99`).

### Что такое `w_min`

`w_min = min_i w_i`. Так как кумулятив `Σ_{k<i} L_r` монотонно растёт по `i`, наименьший вес — всегда
**самое позднее (хвостовое) время**. Значит `w_min` буквально — **«докуда дошёл причинный фронт»**:

| `w_min` | смысл |
|---|---|
| ≈ 1 | хвост полностью включён → фронт прошёл всю область → **сошлось** |
| ∈ (0,1) | фронт в середине области — *градиентный* гейт, поздние времена частично активны — **настоящая причинность** |
| ≈ 0 | хвост выключен — фронт застрял у t=0, поздние времена без градиента — **голодают** |

Цель annealing'а — `w_min > 0.99`. **Критично: чтобы фронт реально маршировал, ранняя `L_r` должна
уметь падать до ~0**, тогда кумулятив сжимается позади фронта. Ненулевой *пол* невязки заставляет
`Σ_{k<i} L_r` расти линейно даже на «сошедшихся» временах → хвост уже не реактивировать. Этот пол —
вся суть ниже.

---

## 2. Глоссарий параметров (как они различаются в двух прогонах)

| параметр | роль | `good_enough` | `new` |
|---|---|---|---|
| `eps_schedule` | свип силы причинности (1.6) | `[5e-4, 1e-3, 3e-3, 5e-3]` (**мягкий**) | `[1e-2, 1e-1, 1, 10]` (**кусачий**) |
| PDE-коллокация `z` | где навязывается поточечная невязка | **полная** `[0,1]` | **только нутро** `[0.06, 0.94]` (грани в маске) |
| `steps_per_eps` (S) | шагов на ε | 10000 | 10000 |
| `causal_delta` (δ) | порог «фронт сошёлся» | 0.99 | 0.99 |
| `Nt` | временных слоёв | 96 | 96 |
| `lr` | learning rate | 3e-4 | 3e-4 |
| `causal_ic` (λ_ic) | вес IC-якоря | 1000 | 1000 |
| веса лосса | mass/mom/energy | 2.0 / 0.25 / 0.5 | 2.0 / 0.25 / 0.5 |

**Различаются только две вещи**: ε-расписание (мягкое vs кусачее) и область PDE-коллокации
(полная vs interior-mask). Всё остальное идентично — сравнение чистое.

Маска нутра добавлена по гипотезе, что пол невязки — это входные/выходные **локальные потери
давления** (`kin,kout`), которые `residuals_scaled(include_local=False)` выкидывает из поточечного
уравнения импульса, но Δp-BC + keeper всё равно их требуют — отчего поточечная невязка в прифланцевых
зонах структурно неудовлетворима. Маска `z∈[0,3α]∪[1−3α,1]` убирает эти точки; грани держатся BC и
**интегральными** keeper'ами (которые интегрируют весь канал).

---

## 3. Результаты — что показывают логи и графики

### 3.1 good_enough (мягкий ε)

- **ε-фазы:** сошлись (w_min>0.99) при ε=5e-4 (186 шагов) и ε=1e-3 (4812 шагов); ε=3e-3 и ε=5e-3
  отработали весь бюджет, w_min на плато **0.96–0.98**.
- **`w_min` держался высоким (~0.97) весь прогон.** График causal-front: `w_i(t) ≈ 1` плоско по всем
  t. **Это НЕ настоящая причинность — это forward-PINN с почти равными весами времён.**
- **`pde` монотонно упал 0.26 → 0.066.** График per-time residual: `L_r(t)` **плоский ~0.07 по всей
  области** — обучен каждый слой.
- **Keeper'ы замкнулись везде:** `mom_int ~1e-3`, `energy_int ~7e-4`, `mass_int ~2e-4`.
- **Валидация:** поток PINN flow-in/out идёт по baseline до ~0.05; NTPC ~3.2 (baseline ~3.4 до
  всплеска); BC держится (π_in≈93, π_out≈0). **Корректная гладкая огибающая** — правильный равновесный
  расход, правильный NTPC, но без DWO.

### 3.2 new (кусачий ε)

- **Ни одна ε-фаза не сошлась** — все четыре упёрлись в бюджет с `w_min < 0.99`.
- **`w_min` схлопывался лесенкой** (график w_min-history): ε=0.01 → ~0.9; ε=0.1 → **~0.46**;
  ε=1 → **~4e-4**; ε=10 → **~1e-12 (фактически 0)**.
- **График causal-front (ε=10):** `w_i(t)` падает в **0 для всех t > ~100 с** — фронт **застрял у
  t=0**, только первые ~5% времён несут вес.
- **Per-time residual ВЫРОС в хвосте:** `L_r(t)` поднимается **0.06 → 0.25** от ранних к поздним
  временам — противоположность плоским 0.07 у good_enough. Голодающий хвост разошёлся.
- **Keeper'ы взорвались** в фазе ε=10: `mom_int 0.02–0.08` и `energy_int 0.06–0.12` — в **~50–150 раз
  хуже** good_enough — потому что хвост получил ноль весов градиента.
- **Валидация:** PINN flow-in **застрял на ~0.075** после 500 с вместо спуска к ~0.05 по baseline;
  flow-out на плато ~0.067; NTPC только **~2.3**. **Хвост неверен** — ровно та зона, которую уморил
  схлопнувшийся фронт.

### 3.3 Решающий контраст

```
                          good_enough (мягкий ε)    new (кусачий ε)
w_min (финал)             ~0.97  (фронт открыт)      ~1e-12 (фронт в t=0)
форма L_r(t)              плоская ~0.07              растёт 0.06 → 0.25 (хвост разошёлся)
keeper'ы (mom/energy_int) 1e-3 / 7e-4               3e-2 / 1e-1   (×50–150 хуже)
хвост потока vs baseline  идёт вниз к ~0.05          застрял ~0.075 (неверно)
NTPC                      ~3.2                        ~2.3
```

---

## 4. Почему `good_enough` лучше — механизм

Результат поначалу парадоксален: прогон, *правильно реализующий причинность* (кусачий ε), **хуже**
прогона, где причинный гейт почти ничего не делает (мягкий ε). Разрешение — **пол невязки**.

1. **Пол не убран.** Маска граней понизила кумулятив (замерено 16.2 → 7.4 на старой сети), но
   **остался реальный интерьерный пол `L_r ≈ 0.08`** — видно прямо в логе `new`: с random init, с
   маской, `pde` упирается в ~0.08 и ниже не идёт. Пол доминирует **несбалансированным энтальпийным
   разгоном на входе** (энергетическая невязка: сеть держит `h` плоской на входе, источник тепла
   `NQ'` нечем скомпенсировать; масса сходится, импульс и энергия — нет).

2. **Кусачий ε на неубранном поле — активно вредит.** При `L_r ≳ 0.08` хвостовой кумулятив
   `Σ ≈ 0.08·96 ≈ 7`, поэтому при ε=1 `w_tail = exp(−7) ≈ 1e-3`, а при ε=10 `≈0`. Фронт
   **схлопывается в t=0 и больше не открывается**, потому что никакое обучение не толкнёт раннюю
   `L_r` ниже пола, питающего кумулятив. Как только `w_tail → 0`, **весь хвост получает ноль
   градиента** — он вообще перестаёт обучаться. Keeper'ы и PDE в хвосте тогда **уезжают и расходятся**
   (`L_r` растёт 0.06 → 0.25; `mom_int`, `energy_int` взлетают). Кусачий гейт выбросил хвост.

3. **Мягкий ε тихо обходит ловушку.** При ε ≤ 5e-3 `w_tail = exp(−5e-3·7) ≈ 0.97`: гейт почти
   равномерный, настоящей причинности он не навязывает — но и **никакое время не морит голодом**. Это
   просто forward-PINN, который обучает все слои, давит `pde` до пола 0.066, замыкает keeper'ы везде
   и садится на правильную гладкую огибающую. Он «выигрывает» именно тем, что **НЕ делает** того, что
   мы хотели.

**Вывод: причинность — точный инструмент, требующий, чтобы пол невязки был ≈0 ЗАРАНЕЕ.** Пока `L_r`
стоит на ~0.08, *любой* гейт, достаточно резкий для навязывания упорядочивания по времени, уморит
хвост и сделает хуже; безопасен только почти-равномерный гейт, а это вообще не причинность. Поэтому
рычаг — **не ε-расписание, а уничтожение пола невязки** (энтальпийный разгон на входе + момент-
формулировка на гранях). Пока `L_r` не сможет достигать ≲0.02 на сошедшихся временах, мягкое
расписание `good_enough` — лучшая рабочая точка.

---

## 5. Дальнейшие шаги (чтобы истинная причинность начала окупаться)

Порядок жёстко задан: **сперва пол, потом гейт.**

1. **Энтальпийный разгон на входе** (доминирующий остаток пола): уплотнить коллокацию сразу за входом
   (`z∈[0.06,0.12]`) и/или поднять вес энергии, чтобы сеть была вынуждена строить `G·h_z ≈ NQ'` от
   z=0, а не держать `h` плоской. Цель — `energy <R²>` с ~0.18 → <0.02.
2. **Момент-формулировка на гранях:** маска помогла, но split поточечной/keeper Δp надо сделать
   самосогласованным (вычесть долю локальных потерь из интерьерного таргета `pi_z`, либо вернуть
   сглаженные локальные потери), чтобы интерьерная `R_mom` могла достигать 0.
3. **Только тогда** снова включать кусачий `eps_schedule=[1e-2,1e-1,1,10]`. При `L_r ≲ 0.02` хвостовой
   кумулятив `≈2`, поэтому ε=1 даёт `w_tail ≈ exp(−2) ≈ 0.14` — **градиентный, марширующий** фронт, а
   не схлоп. Сигнал успеха: `w_i(t)` становится движущейся ступенькой, чья кромка идёт к `t_end`, и в
   хвосте потока развивается DWO вместо гладкой огибающей.

Пока шаги 1–2 не сделаны, держим `good_enough` как продакшен-чекпойнт.
