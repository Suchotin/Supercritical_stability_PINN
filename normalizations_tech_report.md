# Тех-репорт: все нормировки в PINN (`pinn_v3b_imposed_dp_mom_normed copy.ipynb`)

**Дата:** 2026-06-13
**Кейс:** Churkin, Kout=20, Nspc=2.0, supercritical water 25 MPa.

Цель документа — собрать в одном месте *все* масштабы/обезразмеривания, которые есть в коде: что на что делится, зачем, и какие конкретные числа получаются для этого кейса. Числа взяты из печати `Case.build` (cell 5).

Соглашение: «звёздные» величины (`G*`, `h*`, `π*`, `z*`, `t*`) — безразмерные; индекс `0` — опорная (предтранзиентная) рабочая точка; индекс `pc` — псевдокритическая точка.

---

## 0. Опорные константы (псевдокритика при 25 МПа)

Берутся как фиксированные свойства флюида в псевдокритической точке (cell 5, `Case.build`):

| Константа | Значение | Смысл |
|---|---|---|
| `rho_pc` | 317.0348 kg/m³ | плотность в п.к. точке |
| `h_pc` | 2 152 539 J/kg | энтальпия п.к. |
| `Cp_pc` | 76 444.66 J/(kg·K) | теплоёмкость п.к. (пик) |
| `beta_pc` | 0.1285075 1/K | коэф. термич. расширения п.к. |

Геометрия/режим: `L=4.2672 м`, `Dh=8.36e-3 м`, `A=πDh²/4`, `p_ref=p_out=25 МПа`, `dp_ch=0.12 МПа`, `kin=kout=20`, `g=9.81`.

---

## 1. Опорная рабочая точка (индекс 0)

Считается один раз из xlsx-форсинга и EOS:

- `mdot0` — средний расход в предтранзиентном окне (1.0–1.8 с, мощность ≈ 0) = **0.09432 kg/s**
- `G0 = mdot0 / A` — опорный массовый поток [kg/(m²·s)]
- `h0_star = −nspc = −2` → `h0_abs = h_pc + (Cp_pc/beta_pc)·h0_star`
- `rho0 = ρ(p_ref, h0_abs)` = **855.64 kg/m³**, `rho0_star = rho0/rho_pc` = **2.6989**
- `w0 = G0 / rho0` — **опорная скорость** = **2.0082 м/с** (база всего конвективного обезразмеривания)

---

## 2. Независимые переменные (входы сети)

**Физическое обезразмеривание:**
- `z* = x / L ∈ [0, 1]` — координата по каналу.
- `t* = τ · w0 / L` — время в **единицах конвективного транзита** (один проход флюида по каналу = `t*` на 1). `t_end_star` = **846.089**.
  → именно поэтому период DWO измеряется в «транзитах»: `T* ≈ 2.68`.

**Доп. масштабирование на входе NN** (cell 7, `SmoothPINN.forward`) — отдельная вещь, только для обусловленности сети, в физику не входит:
- `z_scaled = 2·z* − 1 ∈ [−1, 1]`
- `t_scaled = 2·t*/t_end_star − 1 ∈ [−1, 1]`

---

## 3. Зависимые переменные (выходы сети → физика, `Physics.fields`)

Сеть выдаёт «шляпы» `Ĝ, ĥ, Π̂` ~ O(0.1); они масштабируются в звёздные поля:

| Поле | Формула в `fields` | Эквивалентное обезразмеривание | Масштаб |
|---|---|---|---|
| `G* = G0_star · Ĝ` | `G0_star = G0/(rho_pc·w0)` | `G* = G/(rho_pc·w0)` | **2.6989** |
| `h* = nspc · ĥ` | `h* = beta_pc/Cp_pc·(h_abs − h_pc)` | энтальпия в единицах `Cp_pc/beta_pc` от п.к. | `nspc=2` |
| `π* = dpi_star · Π̂` | `dpi_star = dp_ch/(rho_pc·w0²)` | давление в долях динамического `rho_pc·w0²` | **93.851** |

Замечания:
- `G0_star == rho0_star == 2.6989` здесь численно совпадают, потому что `G0/(rho_pc·w0) = rho0/rho_pc` (т.к. `G0 = rho0·w0`).
- `h*` отсчитывается ОТ псевдокритики: `h*<0` — «холоднее» п.к., `h*>0` — «горячее». IC при `h* = −2`. Переход через `h*=0` = пересечение псевдокритики (драйвер DWO).
- `π*` нормирован на динамический напор, поэтому целевой перепад `Δπ* = dpi_star = 93.85` — это и есть imposed-Δp в звёздных единицах.

**Плотность и её чувствительность** (`eos25`):
- `rho* = rho/rho_pc` (через IAPWS-сплайн от `h_abs`, `p_ref`).
- `chi_h = d rho*/d h* = (Cp_pc/(beta_pc·rho_pc))·(∂ρ/∂h_abs)` — ключевой множитель: `rho_t = chi_h·h_t`, `rho_z = chi_h·h_z`. Большой `|chi_h|` у псевдокритики = сильная обратная связь по плотности = механизм неустойчивости.
- Аналитический автоград-трюк: `value + chi_h·(h − h.detach())` — даёт точную производную сплайна без двойного дифференцирования.

---

## 4. Безразмерные группы (входят в члены уравнений)

| Группа | Формула | Значение | Где |
|---|---|---|---|
| Фруд `Fr` | `w0²/(g·L)` | 0.09634 | гравитация: `rho*/Fr` |
| Трение `Lambda_ref` | `lambda0·L/(2·Dh)` | 7.026 | `Lambda_dyn = λ_darcy·L/(2Dh)` |
| `Re0` | `G0·Dh/μ0` | 114 091 | вход в `λ_darcy` (Swamee) |
| `λ0` | Swamee(Re0) | 0.02753 | то же |
| Нагрев `NQ'(t)` | `beta_pc/Cp_pc·Q(t)/(rho_pc·w0·A)` | scale 4.914 | форсинг в энергии |

`NQ'` — безразмерная объёмная мощность; это **единственный внешний форсинг** (из xlsx, через `power_torch` интерполяцию по `τ`).

---

## 5. ⭐ Масштабы невязок — «нормировка по точкам»

Это центральная часть. Поточечные невязки PDE считаются в `residuals_raw` (звёздные единицы), затем в `residuals_scaled` **делятся на масштаб своей зоны/уравнения**, и только потом идёт MSE. Цель — сделать каждую невязку O(1), чтобы MSE-лосс не перекашивался в сторону уравнения с самой большой физической величиной (давление ~94 против массы ~2.7 против энергии ~4.9).

| Невязка | Делится на | Значение | Определение |
|---|---|---|---|
| `R_mass` | `mass_scale` | **2.6989** | `max(\|G0_star\|, 1)` |
| `R_energy` | `energy_scale` | **4.9136** | `= NQ_scale = max(\|NQ'\|)` |
| `R_mom` | `mom_mid_scale` | **93.851** | `max(\|dpi_star\|, 1)` |
| (legacy) | `mom_delta_scale` | 1095.66 | `max(dpi_star, local_loss_scale, 1)` |

### Важнейший нюанс этой версии (`mom_normed`)

В `residuals_scaled` **все три зоны** (`in/mid/out`) для импульса делятся на **`mom_mid_scale`** (= `dpi_star`), а НЕ на per-zone `mom_delta_scale`. Причина (из комментариев в `residuals_raw`):

- Локальные потери на торцах (`kin/kout`) — это физически **скачки давления на гранях**, а не распределённая объёмная сила. Гауссова δ-аппроксимация даёт почти-сингулярный спайк `G|G|/ρ_out` в поточечной невязке, который гладкая tanh-сеть не разрешает → завышенная невязка в out-зоне → оптимизатор уходит в «холодный» бассейн.
- Поэтому локальный член **исключён из поточечного `R_mom`** (`include_local=False` в `residuals_scaled`) и перенесён **только в интегральный momentum-keeper**, где δ интегрируется в конечный скачок `½·k·G|G|/ρ` без спайка.
- После удаления спайка внутренняя `R_mom` гладкая во всех зонах → единый масштаб `dpi_star`. Старый `mom_delta_scale ≈ 1096` существовал только чтобы гасить удалённый спайк; оставлен в коде ради совместимости сигнатуры, но **на масштабирование больше не влияет**.

Это и есть смысл суффикса `_mom_normed`: унифицированная по-зонная нормировка импульса.

---

## 6. Интегральные keeper'ы — те же масштабы

Keeper'ы штрафуют проинтегрированные по `z` тождества и используют **те же** масштабы невязок, что и поточечные потери (консистентность — критична, иначе три места разъедутся):

| Keeper | Штрафует | Нормировка |
|---|---|---|
| momentum (`integral_mom_loss`) | `(Σ членов импульса − Δp*)/Δp*` | `dpi_star` (= `mom_mid_scale`) |
| energy (`integral_energy_loss`) | `∫₀¹ R_energy dz` | `energy_scale` |
| mass (`integral_mass_loss`) | `∫₀¹ R_mass dz` | `mass_scale` |

Все усредняют квадрат по выборке времён `_keeper_times` (с уплотнением в последних 10%). Диагностический метод `keeper_time_profiles` считает те же величины как **знаковые профили по времени** на равномерной сетке — `mean(profile²)` совпадает с логируемыми `mom_int/energy_int/mass_int` с точностью до выборки времён.

---

## 7. Веса лосса (НЕ физические масштабы)

Отдельный слой: безразмерные множители на уже-нормированные (O(1)) MSE-невязки (cell 14):

```
mass=2.0   mom=0.25   energy=0.5   ic=50.0   bc=30.0
mom_integral=20.0   energy_integral=20.0   mass_integral=0.0  (инертен)
mom_in=mom_mid=mom_out=1.0
```

Это ручки баланса обучения, а не обезразмеривание. `mom=0.25` маленький, потому что импульс держится в основном интегральным keeper'ом (`mom_integral=20`), а не поточечным членом.

---

## 8. Нормировки IC/BC

- **IC** (`ic_loss`): `R_G = G/G0_star − 1` (относительно рабочего потока), `R_h = h/nspc + 1` (т.к. `h0_star=−nspc` → 0).
- **BC** (`bc_loss`, imposed-Δp по Ambrosini/Churkin):
  - `R_dp = (π_in − π_out)/dpi_star − 1` — фиксируем **перепад** = `Δπ*`, расход свободен (это и есть условие для DWO при постоянном Δp).
  - `R_pi_out = π_out/dpi_star` — калибровка нуля (`p_out=25 МПа` reference).
  - `R_h_in = h_in/nspc + 1` — входная энтальпия.

---

## 9. Сводка «что во что» (одним взглядом)

```
x      ─/L──────────────►  z* ∈ [0,1]      ─(2z*−1)──►  вход NN
τ      ─·w0/L────────────►  t* (транзиты)  ─(2t*/te−1)► вход NN
G      ─/(rho_pc·w0)─────►  G*  = 2.699·Ĝ
h_abs  ─β/Cp·(·−h_pc)────►  h*  = 2·ĥ        (h*=0 ⇒ псевдокритика)
p      ─/(rho_pc·w0²)────►  π*  = 93.85·Π̂
ρ      ─/rho_pc─────────►  ρ*  ; dρ*/dh* = chi_h
─────────────────────────────────────────────────────────────
R_mass   /2.699   (mass_scale = G0_star)
R_energy /4.914   (energy_scale = max|NQ'|)
R_mom    /93.85   (mom_mid_scale = dpi_star)   ← все зоны (mom_normed)
keeper'ы: те же масштабы, интеграл по z, усреднение по t
веса:    множители на O(1)-невязки, не обезразмеривание
```

---

## 10. Где смотреть в коде

| Что | Cell | Объект |
|---|---|---|
| все физ. масштабы | 5 | `Case.build` |
| звёздные поля | 12 | `Physics.fields` |
| EOS / `chi_h` | 12 | `Physics.eos25` |
| форсинг `NQ'` | 12 | `Physics.NQ_prime` |
| поточечные невязки | 12 | `residuals_raw`, `residuals_scaled` |
| keeper'ы | 12 | `integral_{mom,energy,mass}_loss` |
| профили (диагностика) | 12 | `keeper_time_profiles` |
| веса | 14 | `LossWeights(...)` |
| вход NN | 7 | `SmoothPINN.forward` |
