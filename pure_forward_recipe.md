# Pure-forward рецепт обучения PINN: как починить неверный лосс

Документ написан «с нуля»: как обучить сеть так, чтобы она попала в **физически
верное** решение, а не в ложный минимум физического лосса. Все уравнения и функции
потерь выписаны явно. Отдельно разобрано, что значит **«warm-start не на данных»** —
именно это место было непонятным.

---

## 1. Задача

Канал длины `L` с водой при `p ≈ 25 МПа`, около псевдокритической точки. Снизу
подаётся холодная вода с фиксированной энтальпией, по длине её греет мощность `Q(t)`,
которая медленно растёт. На канале **задан постоянный перепад давления** `Δp = 0.12 МПа`,
а **расход свободен**. По мере роста `Q`: выход перегревается → плотность падает →
расход падает; при пересечении порога (NTPC ≈ 2.9) возникают density-wave осцилляции.

Сеть `(z, t) → (G*, h*, π*)` должна выучить эти поля, минимизируя невязки уравнений
(масса, импульс, энергия) + начальные/граничные условия. **Никакой supervised-подгонки
под опорные данные в финальном лоссе нет.**

### Обозначения и константы (звёздные = безразмерные)

| Величина | Символ | Значение | Смысл |
|---|---|---|---|
| Плотность псевдокрит. | ρ_pc | 317.035 кг/м³ | масштаб плотности |
| Энтальпия псевдокрит. | h_pc | 2.1525·10⁶ Дж/кг | масштаб энтальпии |
| Теплоёмкость | Cp_pc | 76444.7 Дж/(кг·К) | |
| Коэф. расширения | β_pc | 0.128507 1/К | |
| Опорная скорость | w0 | 2.00825 м/с | |
| Длина канала | L | 4.2672 м | |
| Площадь сечения | A | 5.489·10⁻⁵ м² | |
| Гидр. диаметр | Dh | 0.00836 м | |
| Опорное давление | p_ref | 25 МПа | |
| Опорный расход | mdot0 | 0.09432 кг/с | холодный стационар |
| Масштаб потока | G0* | 2.69888 | G0/(ρ_pc·w0) |
| **Заданный перепад** | **Δπ\*** | **93.851** | dp_ch/(ρ_pc·w0²) |
| Число Фруда | Fr | 0.096344 | w0²/(g·L) |
| Недогрев на входе | N_spc | 2.0 | h*(0,t) = −N_spc |
| Локальные коэф. потерь | Kin = Kout | 20 | вход/выход |
| Распред. трение | Λ_ref | 7.026 | λ₀·L/(2·Dh) |
| Масштаб импульса (середина) | mom_mid_scale | 93.851 | = Δπ* |
| Масштаб импульса (торцы) | mom_delta_scale | 1095.66 | локальный член велик |
| Масштаб энергии | energy_scale | 4.9136 | масштаб NQ' |
| Масштаб массы | mass_scale | 2.69888 | = G0* |
| Длина транзиента (звёзд.) | t_end* | 846.089 | (= 1797.8 с) |

Поля сети связаны с хатами `(Ĝ, ĥ, Π̂)` линейно:

    G* = G0* · Ĝ        h* = N_spc · ĥ        π* = Δπ* · Π̂

Уравнение состояния (EOS) даёт плотность и её производную **аналитически** (сплайн IAPWS):

    ρ*(h*) = ρ_IAPWS(p_ref, h_abs) / ρ_pc,   h_abs = h_pc + (Cp_pc/β_pc)·h*
    χ_h ≡ ∂ρ*/∂h* = (Cp_pc / (β_pc·ρ_pc)) · ρ_h

Производные плотности по z, t берутся цепным правилом: `ρ*_z = χ_h·h*_z`, `ρ*_t = χ_h·h*_t`
(сплайн **не** дифференцируется autograd'ом — его аналитическая производная `χ_h`
вставляется трюком `value + χ_h·(h* − h*.detach())`).

Источник тепла (форсинг из xlsx — это **вход** задачи, не ответ):

    NQ'(t) = (β_pc / Cp_pc) · Q(t) / (ρ_pc · w0 · A)

---

## 2. Управляющие уравнения

### Масса
    R_mass = ρ*_t + G*_z = χ_h·h*_t + G*_z = 0

### Импульс (Ambrosini 2011, Eq. 8) — шесть членов
    R_mom = G*_t                                   ← инерция (accel)
          + ∂/∂z ( G*² / ρ* )                       ← конвективный поток
          + π*_z                                    ← градиент давления
          + ρ* / Fr                                 ← гравитация
          + Λ · G*|G*| / ρ*                          ← распределённое трение
          + ½·Kin·δ₀(z)·G*|G*|/ρ_in                  ← локальная потеря на входе
          + ½·Kout·δ₁(z)·G*|G*|/ρ_out                ← локальная потеря на выходе
          = 0

где конвективный член раскрывается как `2G·G_z/ρ − G²·ρ_z/ρ²`; распределённое трение
`Λ = λ(Re)·L/(2Dh)` (Darcy, λ от числа Рейнольдса); `δ₀, δ₁` — гауссовы дельты на
торцах (ширина α = 0.02), нормированные на ∫δ dz = 1.

**Важно — анкеринг ρ:** в локальных потерях `ρ_in`, `ρ_out` берутся **на самих торцах**
`z=0` / `z=1` при том же `t`, а не размазанно гауссом. Иначе систематически
недооценивается выходная потеря, которая растёт как `1/ρ_out` при перегреве.

### Энергия (консервативная форма)
    R_energy = ρ*·h*_t + G*·h*_z − NQ'(t) + h*·R_mass = 0

(член `h*·R_mass` — поправка дивергентной формы; при выполненной массе мал.)

### Граничные / начальные условия

**Imposed-Δp постановка** (Ambrosini / Churkin) — перепад задан, расход свободен:

    h*(0, t) = −N_spc                         ← входная энтальпия задана
    π*(0,t) − π*(1,t) = Δπ*                    ← ПЕРЕПАД задан (один на разность!)
    π*(1, t) = 0                              ← калибровка (p_out = 25 МПа)

Начальное условие (только при t = 0):

    G*(z,0) = G0*  (Ĝ = 1)        h*(z,0) = −N_spc  (ĥ = −1)

Расход **нигде** не фиксируется по времени — это и даёт свободу осциллировать.

---

## 3. Почему наивный лосс НЕВЕРЕН (суть проблемы)

Наивный forward-PINN минимизирует только поточечный MSE невязок:

    L_naive = mean[(R_mass)²] + mean[(R_mom)²] + mean[(R_energy)²] + BC + IC

**Этот лосс имеет глобальный минимум в физически НЕВЕРНОМ решении.** Доказано численно
(см. §3 ноутбука `wrong_basin_test.ipynb`): если посадить сеть в *идеальное горячее*
решение и включить `L_naive`, она **сама уезжает в «холодное»** — расход не падает,
выход недогрет. Две причины:

1. **Резкий член `G*²/ρ_out`.** Истинная горячая ветвь имеет почти-сингулярную
   выходную потерю (ρ_out → 0.4). Гладкая tanh-сеть точно её удовлетворить не может →
   там высокая невязка. Холодное решение гладкое → низкая невязка. MSE предпочитает
   гладкое (= неверное).

2. **Поточечный MSE не контролирует интеграл.** Можно сделать `R_mom ≈ 0` в каждой
   точке и при этом нарушить **проинтегрированный** баланс импульса `∫R_mom dz = Δπ*`:
   сеть рисует прямую `π*(z)`, локально балансную, глобально неверную. Аналогично
   энергия: локально малый остаток, но интегрально — недогрев.

**Вывод:** подстройка весов бесполезна (минимум всё равно холодный). Надо
**изменить, ЧТО оптимально**, и **выбрать правильный бассейн** на старте.

---

## 4. Что значит «warm-start не на данных» — ГЛАВНОЕ разъяснение

### Что такое warm-start
Warm-start — это **предобучение регрессией**: до включения физики мы прогоняем
обычный MSE между выходом сети и некоторым **целевым полем** `(G_tgt, h_tgt, π_tgt)`,
чтобы поставить веса в нужную область. Это **только инициализация весов**. Сам по себе
warm-start не остаётся в финальном лоссе.

### «Supervised» ≠ «на данных»
Слово supervised здесь означает лишь «минимизируем MSE до целевого поля». Вопрос —
**откуда взято целевое поле**. Есть два варианта, и они принципиально разные:

| | Целевое поле | Использует ответ (бейзлайн)? | Это data-driven? |
|---|---|---|---|
| §1 (отвергнут) | расход из `Churkin-...xlsx` | **ДА** | **ДА** |
| §5.2 (наш рецепт) | **решение НАШИХ уравнений** (QS-солвер) | НЕТ | **НЕТ** |

В §5.2 цель строится так: мы берём **только входы задачи** — заданную мощность `Q(t)`,
заданный перепад `Δp`, заданную входную энтальпию `h_in`, и EOS — и **численно решаем
наши собственные уравнения** в квазистационарном приближении (root-find по `G*`, см. §5).
Опорный расход из бейзлайна **не трогается вообще**.

То есть мишень warm-start — это **самосогласованное решение тех же PDE, что мы и
решаем**, посчитанное дешёвым редуцированным солвером. Это в точности как в CFD:
нестационарный расчёт стартуют со стационарного решения. **Информация из ответа
(бейзлайна) не утекает.**

### Почему это не «жульничество» и не подгонка
1. Финальный лосс (§2 / §6) **не содержит ни одного supervised-члена** — только физика.
   Решение определяется физикой; warm-start лишь выбирает, в **какой** из физических
   решений (бассейн) оптимизатор скатится.
2. PDE+BC имеют **несколько** решений: ложное «холодное» и истинное «горячее».
   Инициализация выбирает бассейн — она **не смещает ответ** к данным, потому что в
   мишени данных нет; там решение наших же уравнений.
3. Результат можно проверить независимо: после обучения flow обязан совпасть с
   QS-решением наших уравнений (а не с бейзлайном). Если совпал — данные не подмешаны.

**Короче:** «warm-start не на данных» = стартовую точку считаем нашим физическим
солвером из форсинга, а не берём из таблицы с ответом.

---

## 5. Этап 0 — квазистационарный (QS) солвер: чистая физика

Строим целевое поле решением наших уравнений в пределе `∂/∂t = 0`. На каждый момент `t`
ищем такой постоянный по `z` расход `G*`, при котором **интегральный** баланс импульса
равен заданному перепаду.

**Энтальпия из энергобаланса** (квазистационар `G*·h*_z = NQ'`):

    h*(z) = −N_spc + (NQ'(t) / G*) · z          (линейна по z)
    h_abs(z) = h_pc + (Cp_pc/β_pc) · h*(z)
    ρ*(z) = ρ_IAPWS(p_ref, h_abs(z)) / ρ_pc

**Интегральный баланс импульса** (это `∫₀¹ R_mom dz` без члена давления — см. §6):

    MomSum(G*) = G*²·(1/ρ*(1) − 1/ρ*(0))         ← скачок конвективного потока
               + ∫₀¹ ρ*/Fr dz                      ← гравитация
               + Λ_ref · ∫₀¹ G*|G*|/ρ* dz          ← распределённое трение
               + ½·Kin·(G*|G*|/ρ*(0))              ← локальная потеря вход
               + ½·Kout·(G*|G*|/ρ*(1))             ← локальная потеря выход

**Root-find** (бисекция, ~50 итераций) такого `G*(t)`, что:

    MomSum(G*(t)) = Δπ*

Делается на сетке времён (≈80 точек), ~1 секунда. Использует **только** `Q(t)`, `Δp`,
`h_in`, EOS. Результат — целевые поля:

    G_tgt(z,t) = G*(t) / G0*        (Ĝ-цель)
    h_tgt(z,t) = (−N_spc + (NQ'/G*)·z) / N_spc      (ĥ-цель)
    π_tgt(z,t) = 1 − z              (Π̂-цель, линейный профиль)

---

## 6. Функции потерь (полный список)

### 6.1 Поточечные PDE-лоссы (три зоны)
Канал делится на три зоны: `in` z∈[0, 3α], `mid` z∈[3α, 1−3α], `out` z∈[1−3α, 1]
(α = 0.02 → торцевые полосы шириной 6%, где живёт дельта). Невязки масштабируются:

    R_mass  /= mass_scale (2.699)
    R_energy/= energy_scale (4.914)
    R_mom   /= mom_mid_scale (93.85)   в середине
    R_mom   /= mom_delta_scale (1096)  на торцах   ← локальный член реально ~782/1696

(Уравнять масштабы нельзя: на торцах локальный член в ~12× больше, чем в середине.)

В каждой зоне берётся MSE; затем:

    L_mass   = ⅓ Σ_зоны mean[(R_mass/scale)²]
    L_energy = ⅓ Σ_зоны mean[(R_energy/scale)²]
    L_mom    = Σ_зоны w_zone · mean[(R_mom/scale)²]     (w_in=w_mid=w_out=1)

### 6.2 IC-лосс (t = 0)
    L_ic = mean[(Ĝ − 1)²] + mean[(ĥ + 1)²]

### 6.3 BC-лосс (на сетке времён)
    L_bc = mean[(ĥ(0,t) + 1)²]                          ← bc_h_in
         + mean[((π*(0,t) − π*(1,t))/Δπ* − 1)²]         ← bc_dp (перепад задан)
         + mean[(π*(1,t)/Δπ*)²]                         ← bc_pi_out (калибровка)

### 6.4 Momentum-keeper (интегральный) — ЛЕЧИТ блокер из §3.2
Проинтегрируем `R_mom` по z. Член давления даёт `∫π*_z dz = π*(1)−π*(0) = −Δπ*`
(по bc_dp). Значит `∫R_mom dz = 0` эквивалентно:

    MomSum(t) ≡ accel + flux_jump + gravity + friction + local_in + local_out = Δπ*

где (на сетке n_z = 193, для n_t = 16 случайных времён):

    accel     = ∫₀¹ G*_t dz
    flux_jump = [G*²/ρ*]₀¹ = G*²/ρ*|₁ − G*²/ρ*|₀
    gravity   = ∫₀¹ ρ*/Fr dz
    friction  = ∫₀¹ Λ·G*|G*|/ρ* dz
    local_in  = ½·Kin·∫₀¹ δ₀·G*|G*|/ρ_in dz
    local_out = ½·Kout·∫₀¹ δ₁·G*|G*|/ρ_out dz

Лосс — относительное отклонение интегрального баланса от заданного перепада:

    L_mom_int = (1/n_t) · Σ_t [ (MomSum(t) − Δπ*) / Δπ* ]²

Давление сюда **не входит** — оно «сложено» в `Δπ*` через BC. Поэтому keeper заставляет
**сумму физических членов импульса = заданному перепаду** независимо от формы внутреннего
`π*(z)`. Это напрямую выставляет **правильный равновесный расход** (член `local_out ∝
G*²/ρ_out` фиксирует G).

### 6.5 Energy-keeper (интегральный) — УДЕРЖИВАЕТ «горячее»
То же, но для энергии (n_z = 193, n_t = 16):

    R_energy = ρ*·h*_t + G*·h*_z − NQ' + h*·(χ_h·h*_t + G*_z)     (≡ R_energy из §2)
    I(t)     = ∫₀¹ (R_energy / energy_scale) dz
    L_en_int = (1/n_t) · Σ_t I(t)²

Интегрально это глобальный энергобаланс «весь подведённый тепловой поток = прирост
энтальпии потока». **Холодное (недогретое) решение его нарушает, горячее — выполняет.**
Сильный штраф делает горячее состояние выгодным и удерживает сеть от сползания в холодное.

### 6.6 Полный финальный функционал (этап 2)
    J = w_mass·L_mass + w_mom·L_mom + w_energy·L_energy        (поточечные PDE)
      + w_ic·L_ic + w_bc·L_bc                                  (IC + BC)
      + w_mom_int·L_mom_int                                    (momentum-keeper)
      + W_EKEEP·L_en_int                                       (energy-keeper)

Веса: `w_mass=2.0, w_mom=0.25, w_energy=0.5, w_ic=50, w_bc=30, w_mom_int=5, W_EKEEP=30`.

**В J НЕТ supervised/data-члена.** Решение определяется только физикой.

---

## 7. Рецепт обучения «с нуля» (последовательность этапов)

```
Этап 0.  QS-солвер (чистая физика, ~1 c)
         Построить G_tgt, h_tgt, π_tgt root-find'ом MomSum(G*)=Δπ* из Q(t),Δp,h_in,EOS.
         НЕ использует бейзлайн.

Этап 1.  Warm-start = выбор бассейна (~1 мин, supervised, но НЕ на данных)
         opt = Adam(lr=1e-3)
         повторять WARMSTART_STEPS = 800 раз:
             (z,t) ~ U([0,1] × [0, t_end*]),  4096 точек
             (Ĝ,ĥ,Π̂) = model(z,t)
             L_ws = mean[(Ĝ−G_tgt)² + (ĥ−h_tgt)² + (Π̂−π_tgt)²]   ← цель из Этапа 0
             шаг оптимизатора
         Итог: веса в горячем бассейне НАШИХ уравнений.

Этап 2.  Physics + keepers = собственно forward-обучение (~5–8 мин, БЕЗ данных)
         opt = Adam(lr=3e-5)        ← малый lr, чтобы не сорвать warm-start
         повторять KEEP_STEPS = 1500 раз:
             J = total_loss(...)              # §6.6: PDE + IC + BC + momentum-keeper
             Le = energy_keeper(n_t=16)       # §6.5
             (J + 30.0·Le).backward()
             clip_grad_norm(1.0)
             шаг оптимизатора
         Сохранить чекпойнт.
```

Почему именно так:
1. **Этап 0** даёт физически верную мишень из одного форсинга (не из ответа).
2. **Этап 1** ставит сеть в правильный бассейн. Без него (§3) физлосс уедет в холодное.
3. **Этап 2** держит её там двумя интегральными keeper'ами, *меняя* что оптимально:
   - momentum-keeper пиннит `Σ(члены импульса)=Δπ*` → верный равновесный расход;
   - energy-keeper пиннит `∫R_energy=0` → глобальный догрев, делая холодное невыгодным.
   Малый lr + clip не дают сорвать warm-start, пока keeper'ы «застывают» горячую ветвь.

---

## 8. Критерии успеха и ожидаемый результат

- **flow близок к QS-решению наших уравнений** (например, обвал ~0.099 → 0.040) и
  **далеко от холодного бассейна** (0.087/0.081/0.077) → сеть вышла из ложного
  минимума. Это успех **(A1)**.
- **bc_dp** остаётся малым (O(1e-3..1e-4)); `mom_int` и `E_keep` падают.
- **Остаточный зазор ~10% от бейзлайна** — это **(A2), код-ту-код** разброс: наши
  уравнения = Ambrosini, бейзлайн = TEMPA-SC. При `Δp=0.12 МПа` уже в холодном опорном
  состоянии наша модель даёт расход на ~5% другой (0.0896 против 0.0943), на горячем —
  до ~12%. Это **ожидаемо**, не ошибка PINN. «Воспроизвести бейзлайн точно» — неверная
  цель; верная — воспроизвести решение НАШИХ уравнений (QS-солвер).

### Честная оговорка
В `J` поточечный `L_mom` (вес 0.25) тянет к гладкому профилю, а momentum-keeper (вес 5) —
к интегральному балансу. Они конкурируют; поэтому `h_out*` может идти **выше** истинного
энергобаланса (keeper догревает интегрально, оставляя слабину в форме профиля). Это не
ошибка, а компромисс поточечного и интегрального членов. Если flow всё ещё сползает к
холодному — поднять `W_EKEEP` / `KEEP_STEPS` / `WARMSTART_STEPS` (мягко).
